import 'package:flutter/material.dart';

class ContactProperties extends StatelessWidget {
  const ContactProperties({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}